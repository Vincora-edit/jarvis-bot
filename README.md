# Джарвис — AI-ассистент для Telegram

Персональный AI-ассистент с интеграцией Google Calendar, VPN-сервисом, системой бронирования встреч и реферальной программой.

## Возможности

### Календарь и события
- Полная интеграция с Google Calendar через OAuth 2.0
- Просмотр событий: сегодня, завтра, неделя, месяц
- Создание событий голосом или текстом
- Умный поиск событий ("найди встречу с командой")
- Умные напоминания с подготовкой к событию

### AI-функции
- Голосовое управление (Whisper для распознавания)
- Анализ изображений (GPT-4 Vision)
- Контекстная память разговоров
- Вечерняя рефлексия с AI

### Привычки и геймификация
- Трекер привычек с ежедневным прогрессом
- Серии (streaks) и достижения
- XP за выполнение задач
- Уровни и бейджи
- Умные привычки с AI-адаптацией

### Планирование
- Метод 3+3: 3 главных + 3 вспомогательных задачи
- Утренний план дня (8:00)
- Еженедельный обзор (воскресенье, 21:00)
- Фокус-чеки каждые 2 часа

### VPN-сервис (Туннель)
- Собственный VPN на Xray Reality (VLESS)
- Автоматическая генерация ключей
- Subscription URL для автообновления конфигов
- Управление подпиской через бота
- 7 дней бесплатного триала
- Тарифные планы: Basic, Standard, Pro

### Система бронирования (Calendly-style)
- Создание публичных ссылок для записи
- Выбор свободных слотов из Google Calendar
- Автоматическое создание событий
- Уведомления в Telegram о новых бронированиях

### Монетизация и тарифы
- **Бесплатный** — 3 привычки, 3 напоминания/день, 5 AI запросов/день, 7 дней VPN триал
- **Базовый (199₽/мес)** — 5 привычек, 10 напоминаний, 50 AI запросов, 1 VPN устройство, 1 букинг ссылка
- **Стандарт (399₽/мес)** — 10 привычек, 20 напоминаний, 100 AI запросов, 3 VPN устройства, 5 букинг ссылок
- **Про (599₽/мес)** — Безлимит на всё, 5 VPN устройств, AI-инсайты
- Скидки: 3 мес = -17%, 12 мес = -30%
- Реферальная программа: +14 дней за каждого оплатившего друга
- Промокоды с гибкими условиями

---

## Структура проекта

```
jarvis-bot/
├── main.py                 # Точка входа бота
├── config.py               # Конфигурация и переменные окружения
├── create_bot.py           # Инициализация aiogram бота
├── states.py               # FSM состояния для диалогов
├── oauth_server.py         # FastAPI сервер для Google OAuth
│
├── handlers/               # Обработчики команд Telegram
│   ├── __init__.py
│   ├── user.py            # Основные команды пользователя
│   └── tunnel.py          # Команды VPN-сервиса
│
├── services/               # Бизнес-логика (сервисный слой)
│   ├── __init__.py
│   ├── ai_service.py      # Интеграция с OpenAI GPT-4
│   ├── calendar_service.py # Google Calendar API
│   ├── google_oauth_service.py  # OAuth авторизация
│   ├── habit_service.py   # Управление привычками
│   ├── smart_habits_service.py  # Умные привычки с AI
│   ├── memory_service.py  # Контекстная память
│   ├── smart_reminder_service.py  # Умные напоминания
│   ├── marzban_service.py # (Legacy) Интеграция с Marzban
│   ├── vpn_service.py     # VPN сервис (создание ключей)
│   ├── promo_service.py   # Промокоды
│   ├── referral_service.py # Реферальная система
│   ├── limits_service.py  # Лимиты и тарифы
│   ├── plans.py           # Тарифные планы
│   ├── admin_notify_service.py # Уведомления админа
│   ├── cleanup_service.py # Очистка старых данных
│   └── encryption_service.py  # Шифрование токенов
│
├── database/               # Работа с базой данных
│   ├── __init__.py
│   ├── connection.py      # Подключение к SQLite
│   └── models.py          # SQLAlchemy модели
│
├── keyboards/              # Telegram клавиатуры
│   ├── __init__.py
│   ├── actions.py         # Главное меню и кнопки
│   └── tunnel_kb.py       # Клавиатуры VPN
│
├── prompts/                # Системные промпты для AI
│   ├── __init__.py
│   └── system_prompts.py  # Промпты для GPT
│
├── scheduler/              # Планировщик задач
│   ├── __init__.py
│   └── jobs.py            # Регулярные задачи (APScheduler)
│
├── booking/                # Модуль бронирования встреч
│   ├── __init__.py
│   ├── api.py             # FastAPI эндпоинты
│   ├── service.py         # Логика бронирования
│   ├── schemas.py         # Pydantic схемы
│   └── templates/         # HTML шаблоны
│
├── vpn/                    # VPN модуль (Xray Reality)
│   ├── __init__.py
│   ├── config.py          # Конфигурация серверов
│   ├── key_generator.py   # Генератор VLESS ключей
│   ├── subscription.py    # FastAPI subscription API
│   ├── xray_service.py    # Управление Xray сервером
│   └── scripts/
│       └── install_xray.sh # Скрипт установки Xray
│
├── admin-panel/            # Веб-админка
│   └── main.py            # FastAPI админ-панель
│
├── migrations/             # Миграции базы данных
│   ├── __init__.py
│   └── add_smart_habits_fields.py
│
├── scripts/                # Вспомогательные скрипты
│   ├── broadcast_restart.py  # Рассылка сообщений пользователям
│   └── create_calendar.py    # Утилита авторизации Google Calendar
│
├── static/                 # Статические файлы
│   └── css/
│
├── utils/                  # Утилиты
│   └── __init__.py
│
├── deploy.sh              # Скрипт деплоя на сервер
├── requirements.txt       # Python зависимости
├── .env                   # Переменные окружения (не в git)
├── .env.example           # Пример .env файла
├── credentials.json       # Google API credentials
├── client_secret.json     # Google OAuth client secret
├── token.pickle           # Сохранённый OAuth токен
└── bot_database.db        # SQLite база данных
```

---

## Команды бота

### Основные
| Команда | Описание |
|---------|----------|
| `/start` | Запуск бота и главное меню |
| `/help` | Справка по командам |
| `/connect` | Подключить Google Calendar |
| `/today` | События на сегодня |
| `/tomorrow` | События на завтра |
| `/week` | События на неделю |

### Привычки
| Команда | Описание |
|---------|----------|
| `/habits` | Список привычек и прогресс |
| `/habit_add` | Добавить новую привычку |
| `/habit_done` | Отметить выполнение |
| `/stats` | Статистика, XP, уровень |

### Планирование
| Команда | Описание |
|---------|----------|
| `/plan33` | План дня по методу 3+3 |
| `/focus` | Текущий фокус |
| `/diary` | Личный дневник |
| `/unload` | Разгрузка головы |

### VPN и подписка
| Команда | Описание |
|---------|----------|
| `/tunnel` | Управление VPN |
| `/tarif` | Посмотреть тарифные планы |
| `/ref` | Реферальная ссылка |
| `/promo` | Ввести промокод |

### Бронирование
| Команда | Описание |
|---------|----------|
| `/booking` | Создать ссылку для записи |

---

## Кнопки главного меню

- **📋 План** — План дня
- **✅ Привычки** — Трекер привычек
- **🧠 Разгрузка** — Выгрузить мысли
- **⚙️ Режим** — Настройки режима
- **📅 Букинг** — Бронирование встреч
- **🔒 Туннель** — VPN-сервис
- **👥 Рефералы** — Реферальная программа

---

## Установка и настройка

### 1. Клонирование репозитория
```bash
git clone <repo-url>
cd jarvis-bot
```

### 2. Создание виртуального окружения
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```env
# Telegram
BOT_TOKEN=your_telegram_bot_token

# OpenAI
OPENAI_API_KEY=your_openai_api_key

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=https://your-domain.com/callback

# VPN (Xray Reality)
VPN_SERVER_HOST=your-vpn-server-ip
VPN_SERVER_SSH_PORT=22
VPN_SERVER_SSH_USER=root
VPN_SERVER_SSH_PASSWORD=password
VPN_SUBSCRIPTION_SECRET=your-secret-key
VPN_SUBSCRIPTION_DOMAIN=vpn.your-domain.com

# Encryption
ENCRYPTION_KEY=your_fernet_key

# Timezone
TIMEZONE=Europe/Moscow
```

### 5. Настройка Google Calendar API

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект
3. Включите Google Calendar API
4. Создайте OAuth 2.0 credentials
5. Скачайте `credentials.json` и `client_secret.json`
6. Добавьте redirect URI: `https://your-domain.com/callback`

### 6. Запуск

```bash
# Основной бот
python main.py

# OAuth сервер (отдельный терминал)
python oauth_server.py

# Админ-панель (отдельный терминал)
python admin-panel/main.py
```

---

## Деплой на сервер

### Автоматический деплой
```bash
./deploy.sh
```

### Ручной деплой
```bash
# На сервере
cd /root/jarvis-bot
git pull  # или scp файлов
source venv/bin/activate
pip install -r requirements.txt
systemctl restart jarvis-bot
```

### Systemd сервисы

**Бот** (`/etc/systemd/system/jarvis-bot.service`):
```ini
[Unit]
Description=Jarvis Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/jarvis-bot
ExecStart=/root/jarvis-bot/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**OAuth сервер** (`/etc/systemd/system/jarvis-oauth.service`):
```ini
[Unit]
Description=Jarvis OAuth Server
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/jarvis-bot
ExecStart=/root/jarvis-bot/venv/bin/python oauth_server.py
Restart=always

[Install]
WantedBy=multi-user.target
```

---

## Nginx конфигурация

```nginx
# OAuth сервер
server {
    listen 443 ssl;
    server_name djarvis.vincora.ru;

    ssl_certificate /etc/letsencrypt/live/djarvis.vincora.ru/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/djarvis.vincora.ru/privkey.pem;

    # OAuth endpoints
    location /connect {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /callback {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
    }

    # Admin panel
    location /admin {
        proxy_pass http://127.0.0.1:8888;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Booking
    location /book {
        proxy_pass http://127.0.0.1:8081;
        proxy_set_header Host $host;
    }
}
```

---

## База данных

SQLite база `bot_database.db` содержит таблицы:

| Таблица | Описание |
|---------|----------|
| `users` | Пользователи бота |
| `habits` | Привычки пользователей |
| `habit_logs` | Логи выполнения привычек |
| `user_tokens` | Зашифрованные OAuth токены |
| `subscriptions` | Подписки пользователей |
| `tunnel_keys` | VPN ключи |
| `promo_codes` | Промокоды |
| `promo_code_usages` | Использование промокодов |
| `referrals` | Рефералы |
| `booking_links` | Ссылки для бронирования |
| `bookings` | Забронированные встречи |
| `user_schedule_settings` | Настройки расписания |

---

## Технологии

- **Python 3.11+**
- **aiogram 3.x** — Telegram Bot API
- **FastAPI** — OAuth сервер, админка, API бронирования
- **SQLAlchemy** — ORM
- **APScheduler** — Планировщик задач
- **OpenAI GPT-4** — AI-ответы и анализ
- **Whisper** — Распознавание речи
- **Google Calendar API** — Интеграция с календарём
- **Xray Reality** — VPN протокол (VLESS)
- **Jinja2 + HTMX** — Шаблоны фронтенда
- **Fernet** — Шифрование токенов

---

## Порты

| Порт | Сервис |
|------|--------|
| 8080 | OAuth сервер |
| 8082 | Booking API |
| 8083 | VPN Subscription API |
| 8888 | Админ-панель |

---

## Лицензия

MIT License
